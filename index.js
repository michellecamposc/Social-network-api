const connection = require("./database/connection");

//Database connection
console.log("Node API for social Network initialized");
connection();


// Create node server

// Cors config

// Convert the data of the body to js objects